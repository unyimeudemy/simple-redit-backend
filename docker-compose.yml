version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4000:4000"
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_DB: redit-clone2
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "211999"
    ports:
      - "5434:5434"
